<div class="edit">
  <h4>Title:</h4>
  <p><%= @list.title %></p>
</div>

<div class="edit">
  <h4>Body:</h4><p><%= @list.body %></p>
</div>

<%= link_to "Destroy",list_path(@list.id), method: :delete, "data-confirm" => "Are you sure?" %>

<ul>
  <li><%= link_to 'Edit' , edit_list_path(@list.id) %></li>
  <li><%= link_to 'Back' , new_list_path %></li>
</ul>
